# Bitácora - 06 de Noviembre de 2025

## Actividades Realizadas:

- **Configuración de Clave de API de Gemini:**
  - Se creó el archivo `config.js` en la raíz del proyecto.
  - Se añadió la variable `APP_CONFIG.API_KEY` con el valor proporcionado por el usuario.
  - Se verificó que `index.html` ya incluye la carga de `config.js` correctamente.
